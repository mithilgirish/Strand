/**
 * STRAND Mobile Client Configuration
 * 
 * To test the app on a physical device connected to your local WiFi,
 * replace the IP address below with the IPv4 address of your host machine running the FastAPI backend.
 * 
 * Instructions to find your local IP:
 * - Windows: Open command prompt and run `ipconfig`. Look for the "IPv4 Address" of your active Wireless network adapter.
 * - macOS / Linux: Open terminal and run `ifconfig` or `ip route`.
 * 
 * Default: http://192.168.0.100:8000/api/v1
 */
export const API_BASE_URL = 'http://192.168.0.100:8000/api/v1';
